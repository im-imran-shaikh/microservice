#Naming application 
spring.application.name = limits-service

# Connecting spring-cloud-config-server
spring.cloud.config.uri = http://localhost:8888

# Activeing dev profile
spring.profiles.active = dev
